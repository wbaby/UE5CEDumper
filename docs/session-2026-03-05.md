# Session 2026-03-05

## Completed

### PR #142 (dev→main): ScriptStruct Phase A + Game-Thread Dispatch
- **ScriptStruct Phase A**: 12 hardcoded engine struct layouts in `KnownStructLayouts.cs` (FVector, FRotator, FColor, etc.), UE4/UE5 version-aware LWC. InvokeParamDialog struct-aware buffer building + decoding. 36 new tests (255→291).
- **Game-Thread Dispatch**: MinHook submodule (`vendor/minhook`), `GameThreadDispatch.h/cpp` hooks ProcessEvent, queue architecture. ExportAPI integration. Error codes -4/-5/-7.
- **Key fixes**: MSVC SEH isolation (`CallProcessEventSEH` helper), MinHook as separate static C lib (`enable_language(C)`)

### PR #143 (dev→main): ScriptStruct Phase B — Dynamic Struct Layout Discovery
- DLL walks UScriptStruct FField chain via `WalkClass()` for StructProperty params in `WalkFunctions()`
- Returns `struct_fields` inline in `walk_functions` pipe response (no new commands needed)
- UI: `DynamicStructField` model, `DumpService` parsing, `WriteStructParam` overload, `InvokeParamDialog` unified expand (known → dynamic → scalar fallback), `DecodeDynamicStructParamValue()`
- 10 new tests (291→301). Docs: `walk_functions` in pipe-protocol.md.

## Known Issue Found
**LiveWalker UScriptStruct definition drilldown shows empty fields**:
- Repro: DataTable `DT_CoinShopTable` → `RowStruct` (ObjectProperty) → `JackDataTableCoinShop` → empty field table
- Root cause: `RowStruct` is `ObjectProperty` pointing to UScriptStruct **definition object** (type metadata, not instance data). `WalkInstance` reads `ClassPrivate` → gets "ScriptStruct" metaclass → field enumeration returns empty.
- Fix approach: UI-side detect `ptrClassName == "ScriptStruct"` (or `"Class"`) and call `WalkClass(addr)` instead of `WalkInstance(addr)`, showing field definitions.

## Current State
- Branch: `dev`, clean working tree
- PRs #142 + #143 open (dev→main)
- 301 tests green, DLL 991KB, UI 97MB

## Next Steps (priority order)
1. **Fix LiveWalker UScriptStruct drilldown** — detect class/struct definition objects, show field defs
2. **Merge PRs** #142 + #143 to main
3. See `docs/dev-log.md` for full roadmap

## Technical Notes
- `System.Text.Json`: `JsonValue<int>` cannot `GetValue<uint>()` — must cast in test data: `["flags"] = (uint)0`
- MSVC: `__try/__except` cannot coexist with C++ objects requiring unwinding — isolate into separate function
- MinHook `.c` files: MSVC `/TP` flag on C++ target causes C→C++ linkage errors — build as separate static C lib
